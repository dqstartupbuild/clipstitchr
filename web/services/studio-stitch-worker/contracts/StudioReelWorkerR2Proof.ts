export type StudioReelWorkerR2Proof = {
  readonly objectKey: string;
  readonly objectVersion: string;
  readonly sha256Base64: string;
  readonly sha256Hex: string;
  readonly sizeBytes: number;
};
