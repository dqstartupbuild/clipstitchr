export function AppAdShotListGuide() {
  return (
    <section className="bg-surface-muted/45 px-6 py-20 md:py-24">
      <div className="mx-auto max-w-5xl">
        <p className="marketing-eyebrow">Why separate files matter</p>
        <h2 className="marketing-heading mt-5 text-4xl text-text-primary md:text-6xl">
          Capture reusable pieces, not one locked edit.
        </h2>
        <p className="mt-5 max-w-3xl leading-7 text-text-secondary">
          A clean opening, context clip, demo, outcome, and CTA can each do more
          than one job. The list keeps those pieces separate so the paid
          production workflow can test and reuse them later.
        </p>
      </div>
    </section>
  );
}
