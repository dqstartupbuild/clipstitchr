import { DashboardPageHeader } from "@/app/_components/dashboard/DashboardPageHeader";

export function DashboardHeader() {
  return (
    <DashboardPageHeader
      eyebrow="Workspace"
      title="Clipr Dashboard"
      description="Normalize UGC clips and product demos locally, then combine them into downloadable 9:16 videos."
    />
  );
}
